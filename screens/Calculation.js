import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Calculation = () => {
  return (
    <View>
      <Text>This is Calculation screen</Text>
    </View>
  );
};

export default Calculation;

const styles = StyleSheet.create({});
